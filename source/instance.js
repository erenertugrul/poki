"use strict";

{
	const SDK = self.SDK;

	const PLUGIN_CLASS = SDK.Plugins.eren_poki;
	
	PLUGIN_CLASS.Instance = class eren_pokiInstance extends SDK.IInstanceBase
	{
		constructor(sdkType, inst)
		{
			super(sdkType, inst);

		}
		
		Release()
		{

		}
		
		
	};
}